// Importing React
import React from "react";

// Body functional component
const Body = () => {
  // Returning JSX elements
  return (
    <div className="flex justify-center">
      <p className="mx-6">
        Lets learn together. Buddy.ai is powered by AI, so surprises and
        mistakes are possible.
      </p>
    </div>
  );
};

// Exporting Body component as default export
export default Body;
